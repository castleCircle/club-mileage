spring:
  datasource:
    url: jdbc:h2:mem:testdb;mode=mysql;database_to_lower=true
    username: sa
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: none
    properties:
        format_sql: true
        use_sql_comments: true
    show-sql: false
  sql.init.mode: always
